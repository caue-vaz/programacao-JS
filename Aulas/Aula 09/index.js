/*
String: texto;

number: número;

undefined: não coloca valor numa variável, variável vazia;

null: não aponta para lugar nenhum na mémoria;

booleab: valor lógico (true, false);

*/
const nome = 'Caue'; //String
const nome1 = "caue"; //String
const nome2 = `Caue`; //String
const num1 = 10; //number
const num2 = 10.52; //number
let nomeAluno; // undefined --> não aponta pra local nenhuma na memória
const sobrenomeAluno = null; //nulo --> não aponta pra local nenhuma na memória
// const boolean
const aprovado = false; // boolean = true, false (lógico)

let a= 2;
const b= a;
console.log (a,b);//2,2

a = 3;
console.log (a,b); //3,2
